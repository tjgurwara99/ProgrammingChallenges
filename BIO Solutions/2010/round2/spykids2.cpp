#include <cstdio>
using namespace std;

int K, A[32];

struct node { int prev, next, idx; };
node NODES[32];

void bruteForce(int currentNode) {
    
}

int main() {
    
    scanf("%d", &K);
    for (int i = 0; i < K; i++) scanf("%d", &A[i]);
    
    return 0;
}